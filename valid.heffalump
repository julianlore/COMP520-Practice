fun main(s -> string) : void {
     var i -> int = test(34);
     output("hello world" + i);
}

fun test(i -> int) : int {
    if i > 10 {
       return 21;
    } else {
       {
            var a, b -> float = 1.3;
            var c -> float = 0.9;
            var d -> float;
            var e -> string = "h";
            var f -> int = 0;
            var g -> int = 93;
            var weird -> float;
            weird = a ** b + f / -4;
       }
       return 0;
    }
}

fun multi(a -> int, b -> string, c -> float) : int, float, string {
    return a, c, b;
}